# 智能变电站巡检系统 - 环境变量配置示例

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=smart_substation
DB_USERNAME=root
DB_PASSWORD=your_database_password

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# JWT配置 (请使用至少256位的随机密钥)
JWT_SECRET=your_very_long_and_secure_jwt_secret_key_at_least_256_bits_long_for_security

# Token过期时间 (秒)
JWT_EXPIRATION=7200

# 文件存储路径
FILE_UPLOAD_PATH=./uploads

# 服务器端口
SERVER_PORT=8080

# 日志级别
LOG_LEVEL=info
LOG_LEVEL_SPRING=warn

# CORS允许的源 (多个用逗号分隔)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# 注意：
# 1. 生产环境必须修改所有默认密码和密钥
# 2. JWT密钥应使用足够长的随机字符串
# 3. 数据库和Redis密码应使用强密码
